/**
 * Protocol barrel export.
 * Re-exports message types and existing serialization helpers.
 */
export * from './messages';
export { buildNetworkGameState, buildPlayerNetworkState } from '../protocol';

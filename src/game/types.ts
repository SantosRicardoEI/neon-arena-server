/**
 * Re-export all types from shared module.
 * This file exists for backward compatibility — existing game/ files
 * import from './types'. New code should import from '@/shared/types'.
 */
export * from '../shared/types';
